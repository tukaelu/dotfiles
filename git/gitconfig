[init]
  defaultBranch = main

[core]
  excludesfile = ~/.gitignore_global
  quotepath    = false
  editor       = nvim

[color]
  ui     = auto
  diff   = auto
  status = auto
  branch = auto

[push]
  default = simple

[diff]
  tool = nvimdiff
  compactionHeuristic = true

[alias]
  ad  = "!git status --short | fzf | awk '{print $2}' | xargs git add"
  br  = branch
  bra = branch
  ba  = branch -a
  bd  = branch -d
  bm  = branch --merged
  bn  = branch --no-merged
  co  = checkout
  cm  = commit
  di  = diff
  df  = "!git lg | fzf | awk '{print $2}' | xargs -I% git diff %^ %"
  fe  = fetch --all --prune
  lf  = log --graph --pretty=fuller
  ls  = log --graph --decorate --pretty=format:\"%ad [%Cgreen%cn%Creset] %h> %n %Cred%d%Creset %s %n\" --name-status
  ld  = log --graph --decorate --pretty=format:\"%ad [%Cgreen%cn%Creset] <c:%h t:%t p:%p> %n %Cred%d%Creset %s %n\" --stat -p
  lg  = log --graph --pretty=format:'%Cgreen%h %Creset%cd %C(bold blue)[%cn] %Creset%s%C(yellow)%d%C(reset)' --date=relative --decorate --all
  re  = rebase
  ri  = rebase -i
  res = restore
  st  = status
  sw  = switch
  up  = pull --rebase
  # short
  a = ad
  b = br
  c = co
  d = di
  l = ld
  p = up
  s = st

[user]
  name       = Tsukasa Nishiyama
  email      = nsymtks@gmail.com
  signingkey = DBDAF2B46BF96EF6

[github]
  user = tukaelu

[ghq]
  user = tukaelu
  root = ~/Developments/repos

[web]
  browser = open

[commit]
  gpgsign = true

[gpg]
  program = /usr/bin/gpg
